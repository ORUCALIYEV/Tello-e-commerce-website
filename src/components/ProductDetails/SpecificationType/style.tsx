import styled from "styled-components";

const StyleSpecificationType = styled.div`
  h2 {
    margin-bottom: 2.4rem;
  }

  ul {
    display: flex;
    flex-direction: column;
    gap: 1.6rem;
    margin-left: 1.6rem;
  }

  li {
    display: flex;
    justify-content: space-between;
  }
`;

export default StyleSpecificationType;
